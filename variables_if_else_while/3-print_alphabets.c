#include <stdio.h>

int main(void)

{
	char alphabet = 'a', ALPHABET = 'A';
	
	while (alphabet <= 'z')
	{
                putchar(alphabet);
                alphabet++;
		if (alphabet = 'z')
			while (ALPHABET <= 'Z')
			putchar(ALPHABET);
                	ALPHABET++;
	}
        putchar('\n');
return (0);
}
